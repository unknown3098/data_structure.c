createQueue();; = create an empty 0;

isQueueEmpty(Q);;= if (Q is empty) then return true
else return false;

enQuene(Q, item);; = insert item at the rear of Q;

deQueue(Q);; = if (isQueueEmpty(Q)) then return error
else { delete and return the front item Q };

peek;; = if (isQueueEmpty(Q)) then return error
else { return the front item of the Q };

End Queue
